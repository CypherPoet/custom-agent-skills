{
  "skill_name": "session-handoff",
  "evals": [
    {
      "id": 1,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`.\n\nThe user says: \"I've been working on implementing user authentication. I modified src/auth.js to add JWT token validation and updated the middleware. Create a handoff so I can continue later.\"\n\nUse the session-handoff skill to handle this request.",
      "expected_output": "Should trigger the CREATE workflow: run scripts/create_handoff.py with a meaningful slug, confirm the file was created in .agents/handoffs/, prompt the user to fill in the [TODO: ...] sections (especially the 🎯 Next Action line, Current State Summary, Important Context, Immediate Next Steps), and mention the next step is running validate_handoff.py.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`. The setup script seeds at least one prior handoff in .agents/handoffs/.\n\nThe user says: \"I'm continuing the auth work from yesterday. Create a new handoff that links to the previous one.\"\n\nUse the session-handoff skill to handle this request.",
      "expected_output": "Should locate the existing handoff (via list_handoffs.py or by inspecting .agents/handoffs/), then run create_handoff.py with --continues-from pointing at that file. The generated handoff should include a 'Handoff Chain' section with a link back to the predecessor.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`. The setup script seeds a recent handoff in .agents/handoffs/.\n\nThe user says: \"I want to continue where I left off. Load my last handoff.\"\n\nUse the session-handoff skill to handle this request.",
      "expected_output": "Should trigger the RESUME workflow: run list_handoffs.py to surface the candidate, run check_staleness.py and report the level, read the handoff using the Read Strategy (full read for Next Action, Important Context, Pending Work; skim Codebase Understanding and Work Completed), surface the 🎯 Next Action line, and run the Verify Context red-flags pass before doing any other work.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`. The setup script seeds an incomplete handoff in .agents/handoffs/ that still has [TODO: ...] placeholders.\n\nThe user says: \"I created a handoff but I'm not sure if it's complete. Can you validate it?\"\n\nUse the session-handoff skill to handle this request.",
      "expected_output": "Should run validate_handoff.py against the incomplete handoff, report the verdict (NEEDS_WORK), list missing or unfilled sections including the 🎯 Next Action line, and give the user a concrete list of what to fix before the handoff is ready.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`. The setup script seeds an older handoff with several commits made afterward.\n\nThe user says: \"I have a handoff from last week. Is it still relevant or should I create a new one?\"\n\nUse the session-handoff skill to handle this request.",
      "expected_output": "Should run check_staleness.py against the older handoff, report the staleness level (FRESH / SLIGHTLY_STALE / STALE / VERY_STALE), surface the specific drift signals (days old, commits since, branch divergence, missing files), and give the user a concrete recommendation about whether to resume or create a fresh handoff.",
      "files": []
    },
    {
      "id": 6,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`.\n\nThe user says: \"Create a handoff. Note that I configured the API with key sk-abc123xyzabc123xyzabc123xyzabc123xyzabc123xyzabc and the database password = 'supersecret'.\"\n\nUse the session-handoff skill to handle this request.",
      "expected_output": "Should create the handoff, but when running validate_handoff.py the secret-detection patterns must flag the API key and the password. Verdict must be BLOCKED. The agent should warn the user about the security risk, recommend removing the sensitive values, and not treat the handoff as ready until the secrets are scrubbed.",
      "files": []
    },
    {
      "id": 7,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`. The setup script seeds a clean fixture handoff at `.agents/handoffs/<datestamp>-fixture-a-clean-resume.md` whose 🎯 Next Action describes a specific regex fix in `src/auth.js`.\n\nThe user says: \"Pick up where I left off — use the fixture-a-clean-resume handoff.\"\n\nUse the session-handoff skill to handle this request. Report what you would do as the FIRST concrete action. You do not need to actually apply the fix or run tests — describe the first action and why it follows from the handoff.",
      "expected_output": "Should follow the RESUME workflow, locate fixture-a-clean-resume in .agents/handoffs/, read the 🎯 Next Action line, and state that the first concrete action is to edit src/auth.js to change the bearer-prefix regex (adding the missing `+`) so the failing test in tests/auth.test.js passes. The agent should mention reading the failing-test location and the regex change explicitly — these are both in the Next Action line.",
      "files": [],
      "assertions": [
        "Identifies the handoff to resume from (clean-resume fixture)",
        "Surfaces the 🎯 Next Action explicitly or quotes it",
        "Names src/auth.js as the file to edit",
        "References the regex change (adding `+` after `\\s`) or describes the fix in equivalent terms",
        "Mentions running tests / verifying with npm test after the fix"
      ]
    },
    {
      "id": 8,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`. The setup script seeds a chained pair of fixture handoffs: a predecessor (`fixture-b-predecessor`) with the three-phase rollout plan, and a current handoff (`fixture-b-current`) that says backfill is done and Phase 2 is next.\n\nThe user says: \"Continue the migration work — load the most recent fixture-b handoff.\"\n\nUse the session-handoff skill to handle this request. Describe what you would do as the FIRST concrete action. You do not need to actually run any migration commands.",
      "expected_output": "Should follow the RESUME workflow on fixture-b-current, recognize that it links to the predecessor via Handoff Chain, READ the predecessor to find the three-phase plan and the expected dry-run output appendix, and state the first concrete action: run `python migrations/run.py --phase=2 --dry-run` and compare the output against the predecessor's appendix. The agent should explicitly mention reading the predecessor handoff.",
      "files": [],
      "assertions": [
        "Identifies fixture-b-current as the handoff to resume from",
        "Reads or explicitly says it will read the predecessor handoff",
        "Surfaces the Phase 1 → Phase 2 transition context (backfill drained)",
        "Names `python migrations/run.py --phase=2 --dry-run` (or close equivalent) as the first action",
        "Mentions comparing the dry-run output against the predecessor's expected diff/appendix"
      ]
    },
    {
      "id": 9,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`. The setup script seeds a fixture handoff (`fixture-c-gotcha`) that involves a legacy adapter cleanup. The handoff's deferred-items and surface story suggest deleting `src/legacy_adapter.py`, but the Potential Gotchas section explicitly says NOT to delete it because the queue worker imports it via a runtime string lookup.\n\nThe user says: \"Pick up where I left off on the legacy adapter cleanup.\"\n\nUse the session-handoff skill to handle this request. Describe what you would do as the FIRST concrete action. You do not need to apply any changes.",
      "expected_output": "Should follow the RESUME workflow on fixture-c-gotcha, read the 🎯 Next Action line, surface the Potential Gotcha about the runtime string import in workers/queue.py, and state that the FIRST action is to confirm with the user whether the legacy adapter can be removed — NOT to delete the file. The agent should NOT propose deletion as the immediate next step, even though the deferred-items list mentions removal. The agent may also verify the runtime import pattern via grep before talking to the user.",
      "files": [],
      "assertions": [
        "Identifies fixture-c-gotcha as the handoff to resume from",
        "Surfaces the Potential Gotcha about the runtime string import / importlib.import_module",
        "Does NOT propose deleting src/legacy_adapter.py as the first concrete action",
        "First action is asking the user for confirmation (or verifying the runtime import dependency in workers/queue.py)",
        "Names workers/queue.py or the importlib.import_module pattern explicitly"
      ]
    },
    {
      "id": 10,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`.\n\nThe user says: \"Create a handoff for the auth-middleware work. The PRD lives at docs/auth-prd.md and already spells out the full requirements: JWT validation, 1-hour token TTL, refresh-token rotation, and role-based scopes. The source PR is https://github.com/example/repo/pull/847 — its description already covers what shipped this session (validateToken + the rate-limit shim in src/middleware/rate_limit.js). The rationale for picking JWT over server-side sessions is recorded in ADR-0042 at docs/adrs/0042-jwt-over-sessions.md. Next session: wire the rate-limit shim into the Express middleware chain in src/index.js.\"\n\nUse the session-handoff skill to handle this request.",
      "expected_output": "Should run create_handoff.py, then fill the 📚 Source Artifacts section with concrete links (docs/auth-prd.md, docs/adrs/0042-jwt-over-sessions.md, https://github.com/example/repo/pull/847), and write a TERSE Current State Summary / Decisions Made that REFERENCES rather than RESTATES the PRD requirements, the PR diff, or the ADR rationale. The 🎯 Next Action should name src/index.js and the rate-limit shim. Important Context should call out gaps the linked artifacts don't cover, not summarize them.",
      "files": [],
      "assertions": [
        "📚 Source Artifacts section is populated with the PRD path (docs/auth-prd.md)",
        "📚 Source Artifacts section is populated with the ADR reference (ADR-0042 or docs/adrs/0042-jwt-over-sessions.md)",
        "📚 Source Artifacts section references the PR URL (github.com/example/repo/pull/847)",
        "Current State Summary does NOT enumerate the full PRD requirements (JWT validation + 1h TTL + refresh rotation + RBAC scopes) as a list — it stays terse and points to the PRD instead",
        "Decisions Made either links to ADR-0042 or omits the JWT-over-sessions rationale entirely (not restated inline)",
        "🎯 Next Action names src/index.js and the rate-limit middleware step"
      ]
    },
    {
      "id": 11,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`.\n\nThe user says: \"Create a handoff. Next session needs to: (1) scrape the vendor's JavaScript-rendered API docs at https://docs.example-vendor.com/api to extract endpoint definitions, (2) produce a Mermaid sequence diagram showing the auth-then-fetch flow for our integration guide, and (3) draft the README's getting-started section once the diagram is in place.\"\n\nUse the session-handoff skill to handle this request.",
      "expected_output": "Should run create_handoff.py, then fill the 🧰 Skills to Use section with FORWARD-LOOKING recommendations matched to the three upcoming steps. Expected matches from the available skill list: a Firecrawl skill (firecrawl-scrape or firecrawl-instruct) for the JS-rendered scrape, the mermaid-diagrams skill for the sequence diagram, and the crafting-effective-readmes skill for the README section. Each bullet should name a concrete trigger condition tied to the specific step. The skills are NOT used by the current session — they are recommendations for the resuming agent.",
      "files": [],
      "assertions": [
        "🧰 Skills to Use section is filled with at least one real entry (not just `none` and not just TODO placeholders)",
        "Recommends a web-scraping skill (firecrawl-scrape, firecrawl-instruct, or close equivalent) for the JS-rendered docs step",
        "Recommends mermaid-diagrams (or close equivalent) for the sequence diagram step",
        "Recommends crafting-effective-readmes (or close equivalent) for the README draft step",
        "Each recommended skill includes a concrete `when:` trigger condition tied to a specific upcoming step, not a generic description"
      ]
    },
    {
      "id": 12,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`.\n\nThe user says: \"Create a handoff for continuing with our progress in a fresh session.\"\n\nUse the session-handoff skill to handle this request.",
      "expected_output": "Should follow the CREATE workflow even with minimal user input. Should run create_handoff.py to scaffold and lean on auto-populated metadata (recent commits, branch, modified files). Recognize that the user has not supplied enough context to fill load-bearing sections substantively — 🎯 Next Action, Current State Summary, and Source Artifacts cannot be honestly filled from a bare 'continuing with our progress'. Either (a) ask clarifying questions to surface what 'progress' refers to before filling, or (b) fill what can be inferred from auto-populated git/repo state + use `none` for sections genuinely without input + clearly flag the remaining gaps. Must NOT fabricate context — no invented file paths, decisions, gotchas, or restated content from prior eval fixtures masquerading as the user's real work.",
      "files": [],
      "assertions": [
        "Runs create_handoff.py to scaffold (does not hand-author the file from scratch)",
        "Does NOT fabricate a specific 🎯 Next Action containing file paths, function names, or implementation details that the user never mentioned",
        "Either asks the user clarifying questions to disambiguate 'our progress', or honestly marks sections without information as `none` / TODO / explicit gap-flagged rather than filling them with invented content",
        "Where the agent does fill content, it grounds claims in auto-populated metadata (recent commits, modified files, branch) — not in fixture content from prior evals (e.g., does not claim the work is about JWT auth or legacy adapters without evidence in the actual repo state)",
        "Final state is either (a) READY after honest minimal fills with `none` flags, (b) NEEDS_WORK with the 🎯 Next Action left explicitly TODO for the user, or (c) the agent surfaces the ambiguity to the user before completing — but NOT a falsely-confident READY with fabricated content"
      ]
    },
    {
      "id": 13,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`. The setup script seeds most handoffs in the neutral `.agents/handoffs/`, but ALSO seeds one older handoff in the LEGACY `.claude/handoffs/` location named `<datestamp>-legacy-location-resume.md` (written before the skill moved to the neutral directory).\n\nThe user says: \"List my handoffs and then create a new one that continues from the legacy-location-resume handoff.\"\n\nUse the session-handoff skill to handle this request.",
      "expected_output": "Should run list_handoffs.py and surface BOTH the neutral `.agents/handoffs/` handoffs AND the legacy `.claude/handoffs/` one (the read-side unions both locations). Then run create_handoff.py with --continues-from pointing at the legacy-location-resume file; --continues-from must resolve it even though it lives in `.claude/handoffs/`. The newly created handoff must be written to `.agents/handoffs/` (NOT `.claude/handoffs/`) and must contain a populated 'Handoff Chain' section linking back to the legacy predecessor.",
      "files": [],
      "assertions": [
        "list_handoffs.py output includes the legacy `.claude/handoffs/` handoff alongside the `.agents/handoffs/` ones",
        "Runs create_handoff.py with --continues-from referencing the legacy-location-resume filename",
        "The --continues-from link resolves (Handoff Chain shows 'Continues from' pointing at the legacy predecessor, not 'Not found' or 'fresh start')",
        "The newly created handoff file is written under .agents/handoffs/, not .claude/handoffs/",
        "Does NOT claim the legacy handoff is missing or that the skill cannot read it"
      ]
    },
    {
      "id": 14,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`. The setup seeds, among others, a committed chained pair where the predecessor (`<datestamp>-cleanup-superseded-predecessor.md`) is complete and a later handoff `--continues-from` it, plus a complete ~60-day-old `<datestamp>-cleanup-very-stale.md`.\n\nThe user says: \"Clean up my handoffs directory — retire anything that's finished and been moved past.\"\n\nUse the session-handoff skill to handle this request. Identify the cleanup candidates and describe exactly how you would retire each one. Do NOT actually delete or commit anything yet — present the candidates and wait for approval.",
      "expected_output": "Should follow the CLEANUP workflow: run scripts/find_cleanup_candidates.py, then present candidates grouped by tier. The superseded+complete predecessor (cleanup-superseded-predecessor) must appear as a 🔴 strong candidate; the complete very-stale standalone (cleanup-very-stale) as a 🟡 advisory candidate. For removal, the agent should distinguish git-tracked candidates (git rm + commit, recoverable from git history) from untracked ones (trash), using the path the detector reports. It must explicitly wait for per-item approval and NOT delete or commit anything on its own.",
      "files": [],
      "assertions": [
        "Runs scripts/find_cleanup_candidates.py rather than hand-rolling the scan",
        "Surfaces cleanup-superseded-predecessor as a superseded + complete (🔴) retire candidate",
        "Surfaces cleanup-very-stale as a very-stale + complete (🟡) advisory candidate",
        "Explains removal as `git rm` + commit for tracked handoffs (noting git history is the undo) and `trash` for untracked ones",
        "Waits for explicit per-item approval and does NOT delete or commit any handoff unprompted"
      ]
    },
    {
      "id": 15,
      "prompt": "Set up the eval test environment first by running `python3 evals/setup_test_env.py`, then `cd /tmp/handoff-eval-project`. The setup seeds an incomplete handoff (still has `[TODO:` placeholders), a fresh in-progress handoff, and the active tip of a chain (`<datestamp>-cleanup-superseded-current.md`, which continues from an earlier handoff), alongside genuinely retireable ones.\n\nThe user says: \"Prune all the old handoffs — just delete the stale ones.\"\n\nUse the session-handoff skill to handle this request. Describe which handoffs you would and would NOT retire, and why. Do NOT actually delete anything.",
      "expected_output": "Should run scripts/find_cleanup_candidates.py and apply the bias-to-keep rule rather than literally deleting everything old. Must NOT propose retiring: the incomplete handoff (unfinished TODOs), the fresh in-progress handoff, or the active chain tip (cleanup-superseded-current) — even though the user said 'just delete the stale ones'. Should explain that staleness alone is not a retire signal: only completed + superseded (and, advisory, very-stale standalone) handoffs qualify; an old but unsuperseded or unfinished handoff is kept. Must require explicit approval before removing anything, and never auto-delete.",
      "files": [],
      "assertions": [
        "Runs scripts/find_cleanup_candidates.py",
        "Does NOT propose retiring the incomplete handoff (the one with remaining [TODO: placeholders)",
        "Does NOT propose retiring the active chain tip (cleanup-superseded-current) merely for being old",
        "Explains that staleness alone does not qualify a handoff for retirement — completion + supersession is the real signal, very-stale is only advisory",
        "Requires explicit approval and does not delete or commit any handoff unprompted"
      ]
    }
  ]
}
