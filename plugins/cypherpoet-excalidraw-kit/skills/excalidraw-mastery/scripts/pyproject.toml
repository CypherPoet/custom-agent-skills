[project]
name = "excalidraw-mastery-scripts"
version = "0.1.0"
description = "Render helper for the excalidraw-mastery skill (Playwright + headless Chromium)."
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.40.0",
]
